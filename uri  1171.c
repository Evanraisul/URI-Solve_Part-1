#include <stdio.h>

int main()
{
    int n;
    scanf("%d",&n);
    map<int, int> f;
    int t;
    while (n--)
    {
        scanf("%d",&t);
        {
            cout << i.first << " aparece " << i.second << " vez(es)" << endl;
        }
        return 0;
    }
